KUBERNETES_VERSION="1.8.7"

TOPDOMAIN="mysql-operator.ml"

SUBDOMAIN="v${KUBERNETES_VERSION}.${TOPDOMAIN}"

CLUSTER_NAME="test.$SUBDOMAIN"

KOPS_STATE_BUCKET_NAME="${TOPDOMAIN}.kops-state"

KOPS_STATE_STORE="s3://${KOPS_STATE_BUCKET_NAME}/v${KUBERNETES_VERSION}/"

KOPS_VERSION="1.6.0"

KOPS_BIN="kops${KOPS_VERSION}"

PATH="$PATH:./bin"

