KUBERNETES_VERSION="1.16.0"

TOPDOMAIN="mysql-operator.ml"

SUBDOMAIN="prow.${TOPDOMAIN}"

CLUSTER_NAME="$SUBDOMAIN"

KOPS_STATE_BUCKET_NAME="${TOPDOMAIN}.kops-state"

KOPS_STATE_STORE="s3://${KOPS_STATE_BUCKET_NAME}/prow/"

KOPS_VERSION="1.16.0"

KOPS_BIN="kops${KOPS_VERSION}"

PATH="$PATH:./bin"

KUBECTL_BIN="kubectl --context "${CLUSTER_NAME}""

PROW_USER="prow"
